# P10 — OpenAI / OpenWebUI Regression

## 实施

- 运行执行时 main Phase13 predecessor acceptance。
- 新增 query/status/result/history/followup/cancel。
- stream/non-stream。
- plan/input/pause flows。
- disconnect。
- 基于真实 OpenWebUI 版本更新部署文档。

## 阶段门禁

- v0.1 required scenarios zero regression
- real OpenWebUI E2E exact SHA

## GitHub 交付

阶段结束必须：

```text
implementation/evidence complete
→ git diff --check
→ phase commit(s)
→ push
→ verify local HEAD == remote feature HEAD
→ update reports/v0.2/goal-state.json
```

至少一个可识别 `p10` 的阶段提交。大型阶段优先 implementation + evidence 两个提交，
但不能为了形式拆碎无意义提交。

## 必需报告

```text
reports/v0.2/p10-completion.md
reports/v0.2/p10-acceptance.json
reports/v0.2/p10-publication.md
```

失败 required 尝试追加到 `reports/v0.2/failed-attempts/`，不得删除或重标 PASS。
