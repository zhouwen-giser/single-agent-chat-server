# P13 — Docker / Ops / SBOM / Release Candidate

## 实施

- production build/container/Compose clean start。
- PostgreSQL migrations + ready/health。
- OpenWebUI/AG-UI deployment guides。
- source/license/SBOM/secret scan。
- capacity/event-size sanity。
- full v0.2 candidate verify orchestrator。
- known limitations + rollback。
- remote CI exact candidate。

## 阶段门禁

- all required gates zero skip
- remote branch/checks pass

## GitHub 交付

阶段结束必须：

```text
implementation/evidence complete
→ git diff --check
→ phase commit(s)
→ push
→ verify local HEAD == remote feature HEAD
→ update reports/v0.2/goal-state.json
```

至少一个可识别 `p13` 的阶段提交。大型阶段优先 implementation + evidence 两个提交，
但不能为了形式拆碎无意义提交。

## 必需报告

```text
reports/v0.2/p13-completion.md
reports/v0.2/p13-acceptance.json
reports/v0.2/p13-publication.md
```

失败 required 尝试追加到 `reports/v0.2/failed-attempts/`，不得删除或重标 PASS。
