# P03 — Protocol-neutral Principal / Thread / Persistence

## 实施

- 增量实现 conversation_thread/client_thread_binding/interaction_request/interaction_run/agui_interrupt_binding/agent_card_snapshot。
- 从 v0.1 OpenWebUI binding 无损迁移。
- 保留 conversation task binding/A2A event cache/leases。
- 统一 Principal abstraction。
- 覆盖 crash/restart/migration ledger。

## 阶段门禁

- fresh + v0.1 upgrade + restart
- binding preserved
- cross-principal isolation

## GitHub 交付

阶段结束必须：

```text
implementation/evidence complete
→ git diff --check
→ phase commit(s)
→ push
→ verify local HEAD == remote feature HEAD
→ update reports/v0.2/goal-state.json
```

至少一个可识别 `p03` 的阶段提交。大型阶段优先 implementation + evidence 两个提交，
但不能为了形式拆碎无意义提交。

## 必需报告

```text
reports/v0.2/p03-completion.md
reports/v0.2/p03-acceptance.json
reports/v0.2/p03-publication.md
```

失败 required 尝试追加到 `reports/v0.2/failed-attempts/`，不得删除或重标 PASS。
