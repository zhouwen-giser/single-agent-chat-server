# P02 — Unified Interaction Event Spine

## 实施

- 新增 interaction-contract/application。
- 将 string-only coordinator 输出提升为 typed async event stream。
- 保留 bounded stream + getTask polling。
- OpenAI Renderer 从统一事件渲染。
- 实现 sequence/dedupe/safe-public-content policy。
- 保留兼容桥直到 predecessor OpenAI tests 全绿。

## 阶段门禁

- OpenAI contract no regression
- renderer cannot mutate authority

## GitHub 交付

阶段结束必须：

```text
implementation/evidence complete
→ git diff --check
→ phase commit(s)
→ push
→ verify local HEAD == remote feature HEAD
→ update reports/v0.2/goal-state.json
```

至少一个可识别 `p02` 的阶段提交。大型阶段优先 implementation + evidence 两个提交，
但不能为了形式拆碎无意义提交。

## 必需报告

```text
reports/v0.2/p02-completion.md
reports/v0.2/p02-acceptance.json
reports/v0.2/p02-publication.md
```

失败 required 尝试追加到 `reports/v0.2/failed-attempts/`，不得删除或重标 PASS。
