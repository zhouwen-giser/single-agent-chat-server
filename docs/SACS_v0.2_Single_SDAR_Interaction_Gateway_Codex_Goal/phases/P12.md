# P12 — Current SDAR v1.4.x Real E2E

## 实施

- 锁定并运行执行时 exact SDAR main。
- 记录 live Agent Card hash。
- new task/stream/getTask(history)/result/follow-up/cancel。
- Capability/A2A Exposure query。
- Capability gap。
- long task observation end。
- 同一 Task OpenAI/AGUI consistency。

## 阶段门禁

- exact SDAR SHA evidence
- fixture cannot count real

## GitHub 交付

阶段结束必须：

```text
implementation/evidence complete
→ git diff --check
→ phase commit(s)
→ push
→ verify local HEAD == remote feature HEAD
→ update reports/v0.2/goal-state.json
```

至少一个可识别 `p12` 的阶段提交。大型阶段优先 implementation + evidence 两个提交，
但不能为了形式拆碎无意义提交。

## 必需报告

```text
reports/v0.2/p12-completion.md
reports/v0.2/p12-acceptance.json
reports/v0.2/p12-publication.md
```

失败 required 尝试追加到 `reports/v0.2/failed-attempts/`，不得删除或重标 PASS。
