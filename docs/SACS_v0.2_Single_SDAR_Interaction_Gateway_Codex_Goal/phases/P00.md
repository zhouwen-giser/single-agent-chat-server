# P00 — Waiting Gate、执行时来源锁与分支创建

## 实施

- 运行 Waiting Gate；不满足即 WAITING 并结束，不能写产品代码。
- 验证 SACS Phase13 已合 main，main `pnpm verify` 零 required skip。
- 锁定 SACS main SHA/tree/package/migrations/test baseline。
- 锁定 SDAR latest main SHA/version/A2A/Agent Card。
- 锁定 AG-UI official latest release SHA 与 exact npm versions。
- 审计 `@ag-ui/a2a` 与当前 A2A SDK 兼容性。
- 创建/push `feature/sacs-v0.2-agui-interaction-gateway`。
- 创建 `reports/v0.2/goal-state.json`。

## 阶段门禁

- Phase13 merged main proof
- main real verify
- branch base exact
- machine-readable source lock

## GitHub 交付

阶段结束必须：

```text
implementation/evidence complete
→ git diff --check
→ phase commit(s)
→ push
→ verify local HEAD == remote feature HEAD
→ update reports/v0.2/goal-state.json
```

至少一个可识别 `p00` 的阶段提交。大型阶段优先 implementation + evidence 两个提交，
但不能为了形式拆碎无意义提交。

## 必需报告

```text
reports/v0.2/p00-completion.md
reports/v0.2/p00-acceptance.json
reports/v0.2/p00-publication.md
```

失败 required 尝试追加到 `reports/v0.2/failed-attempts/`，不得删除或重标 PASS。
