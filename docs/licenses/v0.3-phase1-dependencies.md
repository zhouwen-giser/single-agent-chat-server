# SACS v0.3 P01 dependency license record

P01 adds one development-only, exactly pinned JSON Schema validator:

| Package | Locked version | License | Scope         | Purpose                                                      |
| ------- | -------------: | ------- | ------------- | ------------------------------------------------------------ |
| `ajv`   |       `8.20.0` | MIT     | devDependency | Validate frozen draft 2020-12 JSON Schemas in contract tests |

No production runtime dependency is added. The existing production license gate
continues to enumerate only `--prod` dependencies and remains unchanged.
