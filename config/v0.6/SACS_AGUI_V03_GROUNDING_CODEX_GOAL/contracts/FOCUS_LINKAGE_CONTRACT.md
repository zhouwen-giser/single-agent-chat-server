# Focus Linkage Contract

Finding, Map Feature, Timeline Item, Choice and Evidence must be navigable through stable public identity.

Preferred linkage:
`findingId -> layerId/featureId -> timelineItemId -> evidenceItemId/sourceProductId`.

Use `FocusTarget` as the normalized navigation object.

Never derive authority from displayName, rank, list index, raw coordinate equality or client ordering.
