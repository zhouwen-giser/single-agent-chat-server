# Required Commands

Codex must discover exact package scripts from the current branch rather than invent command names.

Minimum evidence groups:
1. pre-change existing v0.6 DEVELOPMENT verification;
2. typecheck/lint;
3. focused AG-UI v0.3 contract/adapter tests;
4. Grounding consumer/runtime tests;
5. analysis reference-client/map tests;
6. PostgreSQL persistence/migration tests when touched;
7. local HTTP/SSE AG-UI E2E;
8. full existing v0.6 DEVELOPMENT regression gate.

Real WSGS evidence must use a real source-built or approved deployed WSGS. Fixture/mock/recorded JSON cannot satisfy R01/R02.
