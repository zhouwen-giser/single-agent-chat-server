# S07_E2E_AND_REPORT

## Objective
Run focused + regression + PostgreSQL/local HTTP AG-UI E2E; cover G01-G12; update evidence/report; real WSGS cases only when actually available.

## Exit rule
No phase PASS without command, exit code and bounded evidence. Existing v0.6 tests must not regress.
