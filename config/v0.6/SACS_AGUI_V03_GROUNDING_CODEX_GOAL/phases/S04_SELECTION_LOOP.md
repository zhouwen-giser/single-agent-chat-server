# S04_SELECTION_LOOP

## Objective
Complete WAITING_SELECTION presentation/control contract; exact selector validation; expired/revision-conflict handling; explicit confirmation; new Revision tests.

## Exit rule
No phase PASS without command, exit code and bounded evidence. Existing v0.6 tests must not regress.
