# S06 — recovery and concurrency guards

Status: PASS. Verified source: `1c627ee634b843beab5140d4c719b317a81e2331`.

Command: `SACS_V05_POSTGRES_IMAGE=postgres:17.10-alpine3.23 node scripts/agui-grounding-baseline.mjs S06`; exit 0. Receipt: `receipts/S06-f7350b6d-a42e-4875-b440-05c0ea6c9a6c.json`. Nine gates passed on unchanged committed source: 47 suites / 623 frozen tests, 14 PostgreSQL tests, 10 older v0.5/v0.6 HTTP/SSE E2E tests, typecheck, migration integrity, architecture, lint (0 errors; 187 warnings), build.

State guards reject lower counters, equal-counter conflicts, older Revision/attempt, changed source identity and terminal resurrection. Delta application requires both a valid hash/counter and semantic forward progression. Grounding Activity Snapshot/Delta validates schema, deterministic message identity, matching analysis/Revision/Grounding, monotonic activity revision and truthful status. A stale Activity cannot satisfy the active Revision's recovery requirement. Valid full snapshots may skip counters; duplicate snapshots are harmless.

Transport generations isolate callbacks and partial SSE bytes from disconnected observers. Duplicate/stale RUN_STARTED cannot reset accumulated state. Reconnect remains an explicit full-snapshot observation of an existing analysis, not an upstream cursor or a new Grounding request. Callers must capture the observation generation once per connection as documented in `docs/api/ag-ui-grounding-v03.md`.

G08: recovery unit tests reject stale State/Activity and correctly hashed stale deltas; normal selection integration also rejects old choices. G09: the normal AG-UI integration captures RUNNING before disconnect, verifies no cancellation, lets the existing source finish, rebuilds the SACS composition with the same memory repository, and reconnects to the persisted Revision and full Snapshot without another WSGS request. This is a local HTTP fixture and composition restart, not real process restart or live WSGS acceptance. S07 will add frozen 1.2 PostgreSQL + normal HTTP coverage.

G10: the isolated PostgreSQL test loses the cancellation response, persists CANCEL_REQUESTED, verifies idempotent replay does not repeat cancellation, then observes authoritative CANCELLED and reads the same terminal Grounding Activity from a fresh repository instance. The client independently rejects premature/regressive status changes. This does not claim a live upstream cancellation was captured.

During test development a Native plan fixture initially retained incompatible fields when converted into a Grounding source; it was corrected without relaxing validation. A misplaced cancellation assertion was moved to the intended uncertainty test. Focused tests and the complete phase regression passed afterward.

S05 full remote CI is separately verified in `CI_S05.json`. S07 and R01/R02 remain outstanding; no final completion marker is emitted.
