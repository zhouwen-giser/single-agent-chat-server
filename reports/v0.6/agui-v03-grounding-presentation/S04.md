# S04 — explicit candidate selection loop

Status: PASS. Verified source: `445a1116e6b342031659c0abc0b2bc1d213dec4d`.

Command: `SACS_V05_POSTGRES_IMAGE=postgres:17.10-alpine3.23 node scripts/agui-grounding-baseline.mjs S04`; exit 0. Receipt: `receipts/S04-86e4a0c7-6233-44e0-8287-c2e52c47b3e8.json`. Nine gates passed on unchanged committed source: 45 suites / 585 frozen tests, 14 PostgreSQL tests, 2 v0.6 HTTP/SSE E2E tests, 8 v0.5 HTTP/SSE E2E tests, typecheck, 20-migration integrity, architecture, lint (0 errors; 170 warnings) and build. The older E2E gate is now also mandatory in this phase runner after the S03 full-CI discovery.

The headless client now separates exact-ID candidate inspection from `resolveSelection`. Inspection changes only local draft/focus, with no Control transport. Confirmation must be the boolean `true`; missing, false or string values are rejected before transport. The client checks the current analysis, active Revision, pending intervention lineage/status and current TTL. The server remains the authority, reloading the complete saved source and validating the exact five-field selector. The client-only confirmation gesture is not a new wire field and does not change frozen WSGS bytes.

G04 runs normal AG-UI through the reference client's SSE decoder, inspects a WAITING_SELECTION candidate, rejects unconfirmed submission, sends the existing Analysis Control resolve request, observes a new Revision and Grounding ID, then reconnects and consumes the authoritative Snapshot. HTTP acknowledgement does not modify shared state. Old selection cannot submit after the new Snapshot. Upstream submit counts are exact, and every SDAR operation is prohibited/asserted unused.

G05 demonstrates that an inspected candidate stays readable at expiry, with a disabled presentation and explicit requery affordance, while confirmation rechecks time and submits nothing. Existing AC-025 additionally verifies the server's 410 rejection against its saved authority; existing client tests verify requery omits expired selectors. Local tests also cover unknown candidates, cloned selection state, foreign projection/intervention identities and awaiting-snapshot rejection.

The integrated G04 test exposed a pre-existing observer/recovery mismatch: a reconnect after WAITING_SELECTION was treated as an interrupt resume requiring parentRunId. Explicit `reconnect()` now permits one new same-thread observer Run without inventing a parent, still requiring full State/Activity snapshots. A normal interrupt resume without its parent, wrong-thread reconnect, reused Run and incorrect parent remain rejected. This fixes the S04 closure; it does not claim all S06 recovery/concurrency requirements.

Additional checks: typecheck and 34 focused client/contract tests passed; format and secret checks passed. An initial local HTTP test attempt was blocked by sandbox `listen EPERM` and rerun successfully with local binding available. A direct ESLint invocation incorrectly included an `.mjs` file outside the configured TypeScript project; the corrected TypeScript invocation and `node --check scripts/agui-grounding-baseline.mjs` passed. The required repository lint command passed unchanged.

These are development fixture/memory and isolated PostgreSQL results, not real WSGS acceptance. S05–S07 and R01/R02 remain outstanding. Full CI status must be attached to its own source SHA, not inferred from this receipt.
