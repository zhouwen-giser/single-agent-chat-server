# Grounding presentation closure — S00 inventory

Baseline: `4916a92bebbdb1d83dfe35c070cdedf711e54da2`. Existing implementation and older receipts are context, not PASS for this package.

| Phase | Observed implementation                                                                                                                                                 | Remaining requirement                                                                                                                                                           | Acceptance       |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| S01   | `analysis-repository.projectGroundingSource` persists only sourceStatus/transport; the normal v0.3 handler defaults to `analysis.dag` with one Activity ID per analysis | Frozen Grounding Activity schema, public status projection, per-revision identity, no fabricated progress; actual handler/persistence tests                                     | G01, G10, G12    |
| S02   | Frozen view retains finding IDs, per-feature IDs, evidence IDs and partial timeline/choice links                                                                        | Audit and complete normalized FocusTarget navigation across findings, features, timeline, evidence and choices; prune dangling links under payload limits; no inferred identity | G02, G03, G11    |
| S03   | Reference client supports PAN/ZOOM/HOVER/INSPECT/PIN/UNPIN; localMap is separate from received state                                                                    | FOCUS/FIT_LAYER/FIT_FINDING/TOGGLE_LAYER and validated local geometry/scope drafts; no implicit transport calls or shared mutation                                              | G06, G07         |
| S04   | Frozen selection builder carries exact five-field selector and TTL/revision guards                                                                                      | Explicit confirmation boundary and candidate local-selection lifecycle through normal control to a new Revision; expired options remain readable                                | G04, G05, G08    |
| S05   | Grounding Source Query supports text with CONTINUE/REPLACE                                                                                                              | Validated public geometry/query scope from explicit submission, persisted request identity and new Grounding; no Native compileRevision                                         | G06              |
| S06   | Source pump/recovery and client revision guards exist                                                                                                                   | Verify new Activity identity/schema in recovery, stale run/revision isolation, cancel uncertainty, deduplication and no rerun on reconnect                                      | G08, G09, G10    |
| S07   | Existing frozen suite contains 42 suites/522 tests                                                                                                                      | New focused + PostgreSQL + HTTP/SSE tests and full regression at final source; fresh real WSGS evidence cannot be inferred from prior deployments                               | G01–G12, R01–R02 |

## Baseline command mapping

There is no `verify:v06:development` package script on this baseline. `scripts/agui-grounding-baseline.mjs` runs the existing typecheck, frozen WSGS regression, v0.6 PostgreSQL, v0.6 local E2E, migration, architecture, lint and build scripts without overwriting historical reports. S07 also needs the full CI-required regression and new focused cases; the baseline alone is not final acceptance.

## Authority boundaries

Keep frozen WSGS 1.2 bytes and the existing 1.0/1.1 paths. Generic Native/fixture `analysis.dag` remains valid for real DAGs. Only Grounding Job presentation changes. `UNRESOLVED` may map to UI PARTIAL while its original business status and explanation remain visible. A cancel request is not terminal cancellation. Model/provider phases and progress are never inferred.
