# S01 — truthful Grounding Activity

Verified source: `7a1a7a174c89910970d88aad64eb9d4bdabdec8d`.

Command: `SACS_V05_POSTGRES_IMAGE=postgres:17.10-alpine3.23 node scripts/agui-grounding-baseline.mjs S01`; exit 0. Receipt: `receipts/S01-7cba7a31-978b-4591-a0bd-064df166d038.json`. All eight commands passed on unchanged committed source: 43 suites / 534 frozen tests, 14 PostgreSQL tests, 2 HTTP/SSE E2E tests, typecheck, migrations, architecture, lint and build. Lint has 0 errors and 165 warnings (163 baseline warnings plus two non-null assertions in the handler).

## Implemented

- Frozen `io.sacs/grounding-activity/v1` JSON schema and strict runtime validator. Artifact: `contracts/v0.6/grounding-activity-v1.schema.json`; SHA-256 `7bb5a1f1db12434c5a022466d45d99d41382c60f81b2bbffb35fea70c51b9f0b`.
- Durable source projection carries upstream Grounding ID, Analysis/Revision identity, public business status, mapped UI status and Activity revision. UNRESOLVED remains explicit as sourceStatus while the coarse UI status is PARTIAL.
- Normal Grounding AG-UI snapshots and selection interrupts use `grounding.job` with a deterministic bounded message ID scoped to Analysis + Revision. Generic Native/fixture DAG adapters remain unchanged.
- Local cancellation persists CANCEL_REQUESTED Activity before the upstream cancellation request. Only observed terminal source status replaces it; HTTP success alone does not imply CANCELLED.
- Existing persisted sourceStatus/transport activities have read compatibility using their authoritative source identity and local cancellation state. No internal steps, messages or progress are inferred.
- Shape validation at the AG-UI boundary rejects wrong message identity or undeclared content. Existing guarded Activity deltas remain intact.

## Direct assertions

`v06-frozen-grounding-activity.contract.test.ts`: all eight source statuses, pending/terminal cancellation, per-revision identity, schema validity and rejection, no invented fields, guarded delta and unchanged DAG adapter.

`v06-grounding-lifecycle.postgres.int.test.ts`: stored Activity equals runtime Activity, revision metadata agrees, pending cancellation is durable before upstream confirmation.

`v06-analysis-local.e2e.test.ts`: normal HTTP/SSE path has Grounding Activity, no fake TOOL_CALL, and reconnect reproduces the same final Activity without new upstream work. The frozen 1.2 entry suite additionally rejects DAG/fake TOOL_CALL events in every normal AG-UI scenario.

S02–S07 are not covered by this phase PASS. No final development or real-WSGS marker is emitted.
