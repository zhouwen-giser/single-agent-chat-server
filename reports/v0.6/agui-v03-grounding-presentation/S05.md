# S05 — explicit map scope requery

Status: PASS. Verified source: `43b0785fca7264dc7b8be1c2e6d2a2fa5cd9cf05`.

Command: `SACS_V05_POSTGRES_IMAGE=postgres:17.10-alpine3.23 node scripts/agui-grounding-baseline.mjs S05`; exit 0. Receipt: `receipts/S05-71ed2f7f-a951-4f3c-a38f-663d18f67351.json`. All nine gates passed on unchanged committed source: 46 suites / 602 frozen tests, 14 PostgreSQL tests, 10 v0.5/v0.6 HTTP/SSE E2E tests, typecheck, 20-migration integrity, architecture, lint (0 errors; 177 warnings) and build. Format and secret checks also passed.

`submitNewQuery` is a separate explicit confirmation action. It checks the observed base Revision and displayed draft generation. Generations advance across clear/redraw, preventing stale confirmation from silently submitting replacement geometry. Local drafts remain local after acknowledgement or transport failure; shared scene changes only from authoritative State. There is no implicit request on draw, automatic retry or optimistic authoritative layer.

The existing Source Query command accepts optional validated `queryScope`. Strict validation is applied in the HTTP route, control service, client builder and planner. It bounds finite coordinates, coordinate counts, closed rings and positive radius. The planner sends one existing public MapSelection with command-scoped ID, revision 1 and canonical geometry hash, retaining the exact submitted geometry. Request/semantic hashes include it; reusing the command with changed geometry is a 409 conflict. CONTINUE retains the validated prior Grounding context; REPLACE omits it. Both create a new Revision/Grounding through the existing durable source path, not Native compileRevision.

G06 now covers both modes through normal AG-UI -> reference client draw -> explicit Control POST -> real local HTTP WSGS fixture -> new Revision/Grounding -> reconnect Snapshot. Assertions cover zero initial requests, unconfirmed/stale-draft rejection, exact geometry/hash, 120-second read-only policy, unchanged State until snapshot, replay deduplication, changed-payload conflict and old-Revision draft refusal. SDAR calls are prohibited and asserted unused. Malformed/oversized/authority-injecting scopes are rejected before a second WSGS request.

Point, LineString, Polygon and exact Circle serialization pass the unchanged frozen request validator. Public MapSelection geometry is an opaque object; wire-schema acceptance does **not** establish that live WSGS understands every shape or operation. In particular Circle is not approximated into a polygon or route. Any live unsupported/PARTIAL response must remain visible, not be promoted to spatial business success. See `docs/api/ag-ui-grounding-v03.md` for the reference APIs and this limitation.

A first new geometry test used Node structuredClone inside Jest and hit the frozen validator's intentional prototype check when computing its expected hash. The comparison fixture now uses a JSON roundtrip within the same VM; the frozen validator was not modified. All 13 geometry-planner tests and the complete phase regression passed afterward.

This phase establishes development source-query closure, not new real business acceptance or complete recovery. S06/S07 and R01/R02 remain outstanding. Read-only live capabilities were checked separately: exact 1.2, HTTP 200, six available capabilities, zero business requests. Deployment history was inspected for context only and is not reused as acceptance evidence.
