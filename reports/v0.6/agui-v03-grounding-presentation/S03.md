# S03 — local map interaction and drafts

Status: PASS. Verified source: `82e46be97e8090862f3320ed706165ffdbe75d61`.

Command: `SACS_V05_POSTGRES_IMAGE=postgres:17.10-alpine3.23 node scripts/agui-grounding-baseline.mjs S03`; exit 0. Receipt: `receipts/S03-148a6909-1324-4b39-8dff-082ea7aca7e9.json`. All eight gates passed on unchanged committed source: 45 suites / 573 frozen tests, 14 PostgreSQL tests, 2 HTTP/SSE E2E tests, typecheck, migration integrity, architecture, lint (0 errors) and build. Additional format and secret checks passed. G07 is directly covered; G06 still requires S05 explicit submission evidence.

Implementation: `82e46be`. Headless/reference client now supports all 17 local navigation, inspection, layer visibility and drafting actions. Local pins are renderer overrides, not edits to shared pinnedFocusById. The presentation exposes `shared` as the authoritative scene and `rendered` as its local visual composition. Snapshot arrival preserves local display preferences; neither local action nor rendering increments the server state/scene revision.

Draft Point, LineString, closed Polygon and Circle inputs are bounded and validated. SET/REPLACE update only the local draft; CLEAR removes it. Drafts carry their observed base Revision and a local draft revision. Circle is an exact local draft at this phase, not a claim that a WSGS wire operation has been accepted. S05 still owns explicit submission and public-contract conversion.

FIT only reads explicitly identified existing layers and their published inline geometry; unavailable geometry is an explicit local error. It does not fetch a Provider, synthesize a path or call Analysis Control. Input/state cloning prevents caller-owned objects from mutating retained state. The local dispatcher rejects explicit backend actions such as SUBMIT_NEW_QUERY, RESOLVE_SELECTION and CANCEL_ANALYSIS.

Focused command: typecheck followed by Jest `v06-frozen-local-map.unit.test.ts` and `analysis-reference-client.contract.test.ts`; exit 0, 39 tests. Tests cover all local actions, shared-state/hash preservation, zero fetch calls, display overrides across snapshots, drawing/replacement/clear, invalid geometry/actions, stable-ID FIT and caller aliasing. Changed source lint exited 0.
