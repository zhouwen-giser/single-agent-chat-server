# C04 — Same-source interaction and non-executing historical candidates

## Implemented and reused

The interaction/projection primitives landed with the integrated C03 revision path so the normal routes could exercise them. This phase verifies them specifically against AC-034–038 and adds the missing explicit LineString-preview assertion.

- The existing client receives a validated source view, creates exact five-field Choice resolution commands and semantic source-query proposals, and keeps reading/requery available after expiry. Real-time UI checks are usability checks; the server still authorizes the complete saved source.
- The same Result/hash supplies normal Chat text, AG-UI emitted text/state, map, timeline and Choice anchors. RECONNECT repeats the saved view without a new HTTP query.
- Point and LineString geometry are copied only from public geometry. The new test supplies two distinct published line segments beside a historical trace Gap and checks exact coordinates, two separate layers, non-navigation titles and the original Gap period. It does not infer a time-to-geometry association or insert a bridge.
- Clipping removes dependent actions/layers/items when their findings are absent. Hidden but valid candidates can still be resolved by the normal Control route from complete authorized storage; public hashes are not recomputed for presentation.
- Ordinary Top-K creates no action. Explicit historical candidates retain the actual visited Point, `executionAuthorized=false` and all three follow-up requirements=true; tampering is rejected.
- Historical target and deictic vehicle text cannot create A2A work. Dynamic assertions cover the actual A2A client factory/method spies, while MCP/device isolation is established by no such ports in the composition and the architecture gate—not a claimed dynamic MCP/device spy. Positive ordinary SDAR regression remains enabled.

## Verification

Final `node scripts/v06-frozen-phase-verify.mjs C04`: **exit 0, 110 tests / 6 suites PASS**. Source typecheck, architecture (137 files), migration structure (20 files) and diff checks passed. ESLint: **0 errors / 108 warnings**, preserved in the log. The source digest was stable throughout the successful run. AC-034–038 are now evidenced; REQUIRED ledger is **36/40 PASS, IN_PROGRESS**.

The additional post-staging whitespace check reported the raw ESLint output's final blank line. A narrowly scoped `.gitattributes` rule exempts only end-of-file blank lines in these verbatim command logs; source whitespace checks are unchanged and captured log bytes/digests are not edited.

The first full C04 attempt exited 1: 106 tests passed and four normal-entry tests exceeded Jest's default 20-second per-test budget (all Choice kinds, deictic vehicle text, HTTP 400 and HTTP 406). The full failed Jest output is preserved in C04-attempt-1-command-2.txt; the ledger was not advanced. The normal-entry suite now has a bounded 60-second test budget for its repeated complete-schema/multi-turn work. This changes only the test harness: production HTTP, observation and Choice TTL limits and every assertion remain unchanged. The replacement C04 receipt is valid only after the rerun completes successfully.

The generated C04.json and C04-command logs are authoritative for the actual command outcome. The focused phase selects client/view, all normal-entry HTTP cases, the existing reference client, positive A2A adapter and World application regressions; it also runs full source typecheck, architecture, affected lint, migration structure and diff checks. The final C06 run will execute the full delivered frozen/legacy source plan again.

Real PostgreSQL remains ENV-001 NOT_RUN. No real upstream/model/device, Docker, release build or deployment was used. Frozen public bytes and production implementation were not modified by the LineString test.
