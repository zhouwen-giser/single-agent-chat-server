# C02 — validated world analysis presentation

The normal `normalizeWorldAnalysis` entry now dispatches on saved contract identity into the frozen mapper. The old 1.1 mapper remains isolated. The five public Finding types, choices, typed gaps and optional geospatial component feed deterministic text, map and timeline output only after full public schema, semantic, reference and hash validation.

Timeline items retain original offsets/precision, explicit bounds, source finding/result identities and the complete instant-event extent. Trace execution/period/gap metadata and road network roles remain separate. Road IDs do not invent geometry; sparse off-network previews are points marked non-navigation. FIRST/LAST selection proofs are copied, not recomputed. Ranking preserves medians -40/-50 separately from representative values -42/-45. Action targets retain exact positions and all four restrictive flags. Ordinary ranking does not become an action.

Wire validation uses the public effective result budget; the old 256 KiB payload setting is not applied to the complete wire result. View clipping follows validation and preserves public hashes; the full source stays in Grounding storage. Choice display clipping does not turn candidate IDs into reference-product IDs. The new view byte budget has a 16 KiB metadata floor; existing layer/timeline/candidate limits also apply.

Executed `node scripts/v06-frozen-phase-verify.mjs C02`: 195 tests in 12 suites, typecheck, architecture, and lint (zero errors, 48 warnings). Actual commands and output hashes are in C02.json. Tests include all official positive Result examples, timezone/bounds/precision cases, all six event types, FIRST/LAST confirmed and unconfirmed proofs, road gap classes, large wire results, malformed reference rejection before clipping and safety-flag tampering.

This phase proves mapping, not completed interactive controls. AC-007/008 integrated projection evidence and AC-034–040 normal-entry/interaction evidence remain tracked for C03–C05/C06. No real PostgreSQL/upstream/model/device calls; ENV-001 stays NOT_RUN. No full readiness marker is emitted.
