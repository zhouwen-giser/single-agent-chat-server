# C05 — Normal shared-composition two-turn HTTP closure

## Production path and controlled boundaries

The normal `apps/server/src/main.ts` and the integration suite both use `createV06GroundingAnalysis`. The suite runs `buildServer`, the normal Chat runner, ConversationApplicationService, AG-UI route, actual Analysis Control, GroundingSourceRuntime and the real HTTP adapter. Only persistence/model/network-peer ports are controlled. There is no alternate fake Analysis application or production memory fallback.

The 23 normal-entry cases cover synchronous 200 and asynchronous 202/GET observations, identical snapshot deduplication, source restoration, same-source RECONNECT, both Chat→AG-UI and AG-UI→Chat, all five saved Choice interventions, source proposals, display-only reuse, parent Revision advancement, concurrent/replayed commands, selection scope/hash/TTL failures, hidden candidates, HTTP rejection/uncertainty, projection-crash recovery, old-source lateness, source terminal states and explicit cancellation.

## Captured two-turn relation (artificial public fixtures)

- First real POST yields saved source G0, result hash H0 and finding-set hash F0, bound to analysis A/revision 0/run R0.
- RECONNECT emits the same text/map/timeline and G0/H0/F0 without another HTTP request.
- A normal Choice/START/control request carries exactly `{priorGroundingId: G0, priorResultHash: H0, findingSetHash: F0, choiceId, candidateId}`. The captured second HTTP body also contains the matching `priorGroundings` entry; non-reference candidates do not masquerade as selected ReferenceProduct IDs.
- The second real HTTP result has a different Grounding identity G1. The same analysis advances to revision 1 with parent revision/run lineage. Replay returns the saved command result and does not create another logical query. These symbols describe asserted relationships in test captures, not live identifiers.
- Explicit cancellation first records local intent and invokes the source cancel endpoint once; final state follows the published source observation. Observer disconnect alone never sends cancellation.

## Verification and limits

`node scripts/v06-frozen-phase-verify.mjs C05` exited **0** with **116 tests / 9 suites PASS**, including all 23 normal-entry cases. Full source typecheck, architecture (137 production files), migration structure (20 files) and diff checks passed. ESLint exited 0 with **88 warnings**, retained verbatim. The source-tree digest stayed unchanged. AC-007, AC-008 and AC-039 advance the REQUIRED ledger to **39/40 PASS, IN_PROGRESS**; only final C06 remains.

C05.json and its command logs record the actual focused run. Besides all normal-entry cases, it includes factory-disabled/optional-capability compatibility, stored old-source identity, pump ownership, ordinary OpenAI/predecessor, World application, query service and Control API regressions, plus source typecheck, architecture, affected lint, migration structure and diff checks. C06 separately reruns all delivered frozen and affected legacy suites.

No real PostgreSQL recovery was executed (ENV-001 NOT_RUN). No real WSGS/Provider/model/SDAR/MCP/device, Docker, Native readiness, build, deployment or release gates were invoked. A2A zero-side-effect spies on historical requests coexist with positive ordinary SDAR regression; MCP/device isolation is a composition/architecture property, not a dynamic spy claim.
